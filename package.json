{
    "name": "publicadis-fullstack-startup",
    "version": "3.0.0",
    "scripts": {
        "start": "node server/app.js",
        "heroku-postbuild": "cd client && npm install && npm run build"
    },
    "dependencies": {
        "dotenv": "^16.4.1"
    }
}
